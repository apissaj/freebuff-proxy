{
  "name": "freebuff-proxy",
  "version": "1.0.0",
  "description": "Standalone OpenAI-compatible proxy for Freebuff/Codebuff",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --verbose",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
